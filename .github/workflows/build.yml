name: CI

on: [push, pull_request]

jobs:
  ci:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
        dotnet build
        dotnet test --collect:"Code Coverage"
